<!DOCTYPE html>
<html>
  <head>
    <title>RubyFoodLog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <%= render "shared/header"%>

    <% if notice.present? %>
      <p id="notice" class="notification is-link is-light"><%= notice %></p>
    <% end %>
    <% if alert.present? %>
      <p id="notice" class="notification is-danger is-light"><%= notice %></p>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
